# План апгрейда Quest Dating

> **Контекст:** план составлен после технического аудита (см. `docs/audit.md`).
> **Дата создания:** 2026-05-24
> **Последнее обновление:** 2026-05-31 (после 1.2.3, CI и чистки репо)
> **Правило:** строгий порядок Фаза 1 → 2 → 3. Параллельной работы нет.

---

## Сводка прогресса (на 28 мая 2026)

За пять суток (24–28 мая) закрыто **29 PR** + большая системная чистка
инфраструктуры. См. полную хронологию в `docs/incidents.md` — там два
постмортема: INC-001 (Postgres лежал неделю без алерта, + раздел
"Долгосрочный follow-up") и INC-002 (прод 502 при деплое nginx-конфига,
28 мая — пойман monitor.sh'ом, который как раз и появился после INC-001).

### Что закрыто

**Фаза 1 — безопасность (P0):**
- ✅ 1.1.1 — hardcoded email (PR #1)
- ✅ 1.1.2 — Telegram webhook secret_token (PR #6/#7)

**Фаза 1 — качество кода (P1):**
- ✅ 1.1.3 — .env.example (PR #10)
- ✅ 1.2.1 — удалён мёртвый sitemap-handler (PR #8/#9)
- ✅ 1.2.2 — vitest 2.x → 4.x в client-nuxt (PR #12 → #15)
- ✅ 1.2.4 (6/6 файлов) — синхронизация документации с кодом:
  - 1.2.4a ARCHITECTURE.md (PR #20)
  - 1.2.4c DEPLOY.md (PR #21)
  - 1.2.4d API.md (PR #22)
  - 1.2.4e audit.md (PR #23)
  - 1.2.4f DEVELOPMENT.md + TESTING.md (PR #24)
  - 1.2.4b SETUP.md (PR #31) — закрыт после 1.2.6, как и планировалось
- ✅ 1.2.5 — удалена директория client-legacy/ (PR #11)

**Бонусы (не были в плане, появились по ходу):**
- ✅ Health endpoint реально проверяет БД (PR #2/#3)
- ✅ monitor.sh с реальной проверкой через /health (PR #4)
- ✅ docker-compose правильно передаёт env (PR #7)
- ✅ Postmortem INC-001 (PR #5)
- ✅ Default branch на GitHub: main → production
- ✅ Восстановление БД после недели downtime (INC-001)
- ✅ Удалён orphan SearchBar.test.ts (PR #14)
- ✅ npm audit fix: 5 vulnerabilities → 0 в client-nuxt (PR #16)
- ✅ Docker base image: node:20-alpine → node:22-alpine (PR #17)
- ✅ Host Node 20 → Node 22 LTS через nvm (системная чистка)
- ✅ PM2 полностью устранён (рудимент pre-Docker-эпохи)
- ✅ Дублирующая apt-копия Node удалена
- ✅ package-lock.json теперь трекается в git (PR #13)

### Где мы сейчас

Фаза 1 — **~95% закрыта**. Документация синхронизирована полностью
(6/6 файлов), БД и dump.sql закрыты, nginx и публичный /health починены
(включая разбор INC-002). 31 мая закрыты обе крупные оставшиеся задачи:
- **1.2.3 ✅** — integration-тесты переведены на реальную PostgreSQL через
  testcontainers (PR #36/#38/#40 + фундамент #35). Все 5 API-файлов:
  health/orders/quests/telegram на реальной БД, auth/contact остаются
  unit-мок-тестами (в БД не ходят). Integration 45/45, unit 56/56.
- **1.3.1 (частично) ✅** — CI через GitHub Actions (PR #41): job unit +
  integration на каждый PR/push в production. Branch protection требует оба
  чека зелёными перед merge. Подробности и сознательные урезания — ниже.

Осталось до конца Фазы 1:
- **1.3.1 (дозакрыть)** — job lint (server+client), опц. build client,
  и **1.3.2** (husky/lint-staged). См. примечание про client-тесты ниже.
- **1.4** — мелкие follow-up (cache metrics, uptime pinger, nginx-гигиена
  из INC-002, и т.д.)
- **1.5** — техдолг: 1.5.3 ✅ / 1.5.4 ✅ (чистка репо), остальное открыто;
  **1.5.1 (orders) сознательно отложена в Фазу 2** — см. примечание.

### Примечание: что в CI делаем и чего НЕ делаем (решено 31 мая)

1.3.1 в исходном плане предполагал 4 job: lint, test-server, test-client,
build. Реализовали пока только test-server (разбит на unit+integration).
Сознательные решения по остальному:

- **test-client в CI НЕ добавляем** до стабилизации фронта. Причина та же,
  что и для «не пишем e2e на плеер до Фазы 3»: фронт целиком входит в зону
  переделки (Фаза 2 заменяет форму заказа на AI-чат, Фаза 3 — редизайн
  плеера). Существующие 166 client-тестов (1.2.2) под branch protection
  начнут блокировать merge ровно тогда, когда будем ломать компоненты.
  Серверной части переделка дизайна не касается — поэтому серверный CI
  оправдан и стабилен, а клиентский преждевременен. Вернуться к нему —
  после стабилизации фронта (реалистично конец Фазы 3).
- **lint — добавляем** (ортогонален дизайну, новый код тоже должен
  проходить ESLint; сюда же 1.4.5 — ревизия конфигов под Node 22).
- **build client — кандидат** (проверяет «фронт вообще собирается», не
  привязан к конкретным компонентам), но во время активной переделки
  промежуточные коммиты могут не собираться и мешать под защитой — на
  усмотрение, не обязателен.

### Следующий логический шаг

**Дозакрыть CI (продолжение 1.3.1 + 1.3.2):** добавить job lint, при
желании build client, и pre-commit hooks (husky + lint-staged). Это
завершает инфраструктуру и не зависит от переделки фронта. Затем мелочи
1.4 (документация/nginx-гигиена) и оставшийся техдолг 1.5.

---

## Стратегия (без изменений)

1. **Сначала фундамент.** Пока в коде есть P0-уязвимости и мокированные тесты,
   любая продуктовая фича рискует сломать прод или утечь данными.
2. **Потом главная фича.** AI-опросник через DeepSeek — единственное реальное
   конкурентное преимущество, важнее редизайна.
3. **Потом продукт.** Редизайн плеера, сценарии с фото, аудит мини-игр —
   делается после того, как опросник стабильно работает и даёт нормальные данные.

---

## Фаза 1 — Технический фундамент

### 1.1. Безопасность (P0)

#### ~~Задача 1.1.1. Убрать hardcoded email~~ ✅ DONE (PR #1)

Fallback `'vp.vlad00@mail.ru'` заменён на чтение из `NOTIFY_EMAIL`.
При отсутствии переменной — warning + не отправляется.

#### ~~Задача 1.1.2. Верификация Telegram webhook~~ ✅ DONE (PR #6/#7)

Webhook проверяет заголовок `X-Telegram-Bot-Api-Secret-Token` против
`TELEGRAM_WEBHOOK_SECRET` из env. При несовпадении — 401. Если переменная
не задана — warn + пропуск (backward compat). PR #7 добавил передачу
переменной в docker-compose.

#### ~~Задача 1.1.3. Дополнить .env.example~~ ✅ DONE (PR #10)

Добавлены: `TELEGRAM_WEBHOOK_SECRET`, `RESEND_API_KEY`, `NOTIFY_EMAIL`.
Убраны мёртвые: `RATE_LIMIT_*`, `MAX_FILE_SIZE`. Исправлен `UPLOAD_DIR`
→ `UPLOADS_DIR` (опечатка приводила к тому, что переменная никогда не
читалась). Закомментированы опциональные: `SMTP_*`, `FRONTEND_URL`.

### 1.2. Качество кода (P1)

#### ~~Задача 1.2.1. Удалить мёртвый sitemap-urls~~ ✅ DONE (PR #8/#9)

`client-nuxt/server/api/sitemap-urls.get.ts` удалён. Активный
`server/routes/sitemap-urls.get.ts` продолжает работать. Бонусом
обработан `robots.txt` → `_robots.txt` (npm run build переименовал).

#### ~~Задача 1.2.2. Разобраться с vitest~~ ✅ DONE (PR #12 → #15)

Vitest в client-nuxt поднят 2.x → 3.x → 4.1.7. Выровнен с server
(оба теперь на 4.x). Тесты проходят: 3 файла, 166 тестов, 0 failed.
По пути удалён orphan-тест SearchBar.test.ts (PR #14) и сделан
npm audit fix (PR #16).

#### ~~Задача 1.2.3. Реальные integration-тесты~~ ✅ DONE (PR #35/#36/#38/#40)
- **Что вышло на практике** (отличия от исходного замысла зафиксированы):
  1. ✅ Подключён `@testcontainers/postgresql` — поднимает `postgres:15-alpine`
     (вариант с docker-compose.test.yml не понадобился).
  2. ✅ Реальный pool вместо `vi.mock` — через `tests/helpers/integration-db.js`
     (`setupIntegration`/`teardownIntegration`), контейнер на каждый файл.
  3. ⚠️ **Изоляция НЕ через транзакцию с rollback**, как планировалось, а через
     `reset()` = DROP schema CASCADE + повторное применение dump между тестами.
     Транзакционный rollback не подошёл: код использует собственный pool и
     fire-and-forget вызовы, обернуть всё в одну транзакцию надёжно нельзя.
     reset медленнее, но детерминирован и не зависит от внутренностей кода.
  4. coverage thresholds — пока не трогали (отдельно при дозакрытии CI).
- **Раздельные конфиги:** `vitest.config.js` (unit, мок-БД) и
  `vitest.integration.config.js` (реальная БД, setup.integration.js мокает
  только лимитеры+нотификации). Скрипты `test:unit` / `test:integration`.
- **Покрытие:** health/orders/quests/telegram — на реальной БД (45 тестов).
  auth/contact в БД не ходят — корректно остаются unit-мок-тестами.
- **Бонусы по ходу:** integration-тесты вскрыли и зафиксировали два
  пред-существующих бага — утечку PII (полный телефон клиента в Telegram,
  PR #37) и расхождение с тест-спекой contact-уведомления (PR #39).
- **Ветка(и):** upgrade/integration-testcontainers, -orders, -quests, -telegram

#### Задача 1.2.4. Обновить документацию (расширено, разбито по файлам)

**Контекст:** после двух суток изменений много файлов документации
расходятся с реальностью. Разбито на под-задачи по файлам — каждая
самостоятельна, можно делать в любом порядке.

##### 1.2.4a — Синхронизировать ARCHITECTURE.md с кодом
- **Файл:** `docs/ARCHITECTURE.md`
- **Что исправить:**
  1. `/quest/**` — теперь SSR (или, если решим вернуть CSR — поменять `nuxt.config.ts`)
  2. Nginx — не Docker-сервис, а внешний процесс на VPS
  3. Health endpoint теперь проверяет БД, отдаёт 503 при отказе
  4. Telegram webhook требует secret_token
  5. Сверить остальные расхождения
- **Ветка:** `docs/sync-architecture`

##### 1.2.4b — Обновить SETUP.md
- **Файл:** `docs/SETUP.md`
- **Что исправить:**
  1. Node 22 LTS (а не 20)
  2. Полный список env-переменных (синхронизировать с актуальным .env.example)
  3. Команда setWebhook теперь требует `secret_token`
  4. После 1.2.6: инструкция, как поднять БД с нуля из dump.sql
- **Ветка:** `docs/sync-setup`

##### 1.2.4c — Обновить DEPLOY.md
- **Файл:** `docs/DEPLOY.md`
- **Что исправить:**
  1. Текущий workflow: ветки → PR → merge → `git pull` + `docker compose build && up`
  2. Уточнить когда нужен `--no-cache` (например, при смене base image)
  3. Когда нужен build, когда достаточно `up -d` (env-only changes)
  4. Откат через `git revert` или git tag
- **Ветка:** `docs/sync-deploy`

##### 1.2.4d — Обновить API.md
- **Файл:** `docs/API.md`
- **Что исправить:**
  1. `/health` теперь возвращает `db:connected`/`disconnected`, коды 200/503
  2. `/api/telegram/webhook` требует заголовок `X-Telegram-Bot-Api-Secret-Token`
  3. Сверить остальные endpoints с актуальным `server/src/routes/`
- **Ветка:** `docs/sync-api`

##### 1.2.4e — Прометить пройденные пункты в audit.md
- **Файл:** `docs/audit.md`
- **Что:**
  1. Все закрытые задачи Топ-10 пометить `~~strikethrough~~` или `✅ DONE`
  2. Добавить ссылки на PR
  3. Удалить пункты, которые уже неактуальны (например, про vitest 4.0.18)
- **Ветка:** `docs/update-audit`

##### 1.2.4f — Обновить DEVELOPMENT.md и TESTING.md
- **Файлы:** `docs/DEVELOPMENT.md`, `docs/TESTING.md`
- **Что исправить:**
  1. DEVELOPMENT: Node 22, package-lock.json теперь в репо, обновлённый список зависимостей
  2. TESTING: vitest 4.x, текущая структура тестов, mock-БД vs планируемые real integration tests (отсылка к 1.2.3)
- **Ветка:** `docs/sync-dev-test`

#### ~~Задача 1.2.5. Удалить client-legacy/~~ ✅ DONE (PR #11)

135 файлов / 4.9 МБ старого Vue-фронтенда удалены. Git tag
`pre-cleanup-legacy` создан перед удалением — можно восстановить
через `git checkout pre-cleanup-legacy -- client-legacy/`.

#### Задача 1.2.6. Привести в порядок БД: схемы и dump.sql
- **Контекст:** в `database/` четыре файла:
  - `schema_v2.sql`
  - `schema_v2_utf8.sql`
  - `schema_clean.sql`
  - `full_dump.sql`

  Непонятно, какой каноничный. `docker-compose.yml` монтировал
  `./database/dump.sql` при первом старте postgres, но **файла нет в репо** —
  свежий клон даёт пустую БД. **Это причина INC-001** — Docker создал
  пустую директорию на месте отсутствующего файла. Сейчас bind-mount
  закомментирован (PR #4), но это временное решение.

- **Что:**
  1. **Аудит схем:** сравнить все четыре файла через diff, выяснить
     какой соответствует production-БД (через `pg_dump --schema-only` на
     живой БД и сравнение)
  2. **Выбрать одну каноничную схему** — например, `schema.sql` (без суффиксов)
  3. **Удалить остальные** (с git tag `pre-schema-cleanup` для отката)
  4. **Сгенерировать `dump.sql`** для свежей установки:
     - `pg_dump --schema-only` + seed-данные (категории, базовые шаблоны)
     - **БЕЗ production-данных клиентов** (это PII)
     - Положить в `database/dump.sql`, добавить в репо
  5. **Раскомментировать** bind-mount в docker-compose.yml
  6. **Обновить `docs/SETUP.md`** (или объединить с задачей 1.2.4b):
     инструкция, как разворачивать БД с нуля
  7. **Проверить:** `docker compose down -v && docker compose up` — БД
     создаётся корректно из dump.sql
- **Тест:** ручной — свежий запуск контейнера, проверка структуры таблиц
- **Ветка:** `upgrade/db-cleanup`
- ⚠️ **Блокер для развёртывания с нуля** — пока не сделано, новый VPS
  поднять "одной командой" нельзя.

### 1.3. Инфраструктура (P2)

#### Задача 1.3.1. CI/CD через GitHub Actions — 🟡 ЧАСТИЧНО (PR #41)
- **Файл:** `.github/workflows/ci.yml` (создан)
- **Сделано:**
  - ✅ На каждый PR и push в production
  - ✅ Job `unit` (`test:unit`, мок-БД, fail-fast) + job `integration`
    (`test:integration`, реальная PostgreSQL через testcontainers,
    пред-пул postgres:15-alpine). Оба прошли в чистом CI с первого раза.
  - ✅ Branch protection (ruleset `protect-production`): оба чека required,
    require-PR, up-to-date, block force pushes. Прямой push в production
    невозможен, красный CI блокирует merge.
- **Осталось (дозакрыть отдельным PR):**
  - Job `lint` (ESLint server + client) — сюда же 1.4.5
  - Job `build` (client-nuxt) — опционально, см. примечание в шапке
  - ❌ Job `test-client` — **сознательно НЕ делаем** до стабилизации фронта
    (см. «Примечание про CI» в начале файла)
- **Ветка:** `upgrade/ci-cd` (для дозакрытия)
- **Зависит от:** 1.2.3 ✅, lock-файлы ✅ (PR #13)

#### Задача 1.3.2. Pre-commit hooks
- **Что:** husky + lint-staged
- **Проверки:** ESLint на staged файлы, prettier format check
- **Ветка:** объединить с 1.3.1

### 1.4. Follow-up задачи (появились по ходу работы 24–26 мая)

Мелкие задачи, всплывшие во время инцидента и зачистки. Не блокеры,
но улучшают наблюдаемость и архитектуру.

#### ~~Задача 1.4.1. Логировать cache hit/miss в cache.js~~ ✅ DONE (PR upgrade/cache-metrics)
- В `cache.js` добавлены счётчики `hits`/`misses` (инкремент в getOrSet),
  `stats()` отдаёт `hits`/`misses`/`hitRate` (% с защитой от /0), sweep-интервал
  (5 мин) пишет лог `Cache stats` при активности. `clear()` сбрасывает счётчики.
  Тесты: `tests/unit/utils/cache.test.js` (7 кейсов).
- **Замечено:** `cache.stats()` не вызывается ни /health, ни metrics — задел
  есть, наружу по HTTP не выведен. Лог покрывает видимость; HTTP-эндпоинт
  `/api/metrics` — отдельный follow-up при необходимости.

#### ~~Задача 1.4.2. Расширить /health (Telegram + Resend)~~ ✅ DONE — вариант A (PR upgrade/health-external-deps)
- **Что сделано:** `/health.services = { telegram, resend, notifyEmail }` со
  значениями `configured`/`not_configured`. Проверяется **настроенность**
  (наличие env), зеркаля реальную логику notificationService (telegram —
  И BOT_TOKEN, И CHAT_ID; resend — RESEND_API_KEY). Ловит частый класс
  деплой-ошибок (забыли/опечатали переменную).
- **Контракт сохранён:** при живой БД код 200 и `db:connected` неизменны
  (monitor.sh парсит их), services добавлено рядом. Тесты +5 кейсов.
- **⚠️ Отличие от исходного замысла:** живость («ключ есть, но сервис лёг»)
  НЕ проверяется. Активный пинг внешних API в /health — антипаттерн: это
  горячий путь мониторинга (monitor.sh + pinger каждые 1-5 мин), пинг
  замедлял бы health и жёг квоты. Поэтому верхний `status` НЕ переключается
  в `degraded` по внешним сервисам (это вводило бы в заблуждение — проверена
  лишь настроенность, не живость).

#### Задача 1.4.2b. Пассивный трекинг живости Telegram/Resend (отложена)
- **Идея:** notificationService уже шлёт реальные запросы (нотификации,
  письма). Завести лёгкий счётчик «последний вызов: успех/ошибка/когда»,
  обновлять при реальных отправках, /health читает состояние без новых
  запросов. Честнее варианта A (живость, не только настроенность), и
  «бесплатно» в горячем пути (без лишних запросов).
- **Почему отложена:** ловит более редкий класс (сервис лёг при верном
  ключе) против частого деплой-класса, что закрыт в A. Делать после того,
  как появится потребность; сейчас лог-ошибки отправок + A достаточно.
- **Ветка:** `upgrade/health-liveness-passive` (когда дойдём)

#### Примечание: lint-кластер отложен (решено 31 мая)
Задачи **lint-job в CI**, **1.3.2 (husky/lint-staged с ESLint)** и **1.4.5
(ревизия ESLint-конфигов)** — это ОДИН кластер, завязанный на починку
ESLint-конфига фронта. Прогон `eslint .` дал 333 проблемы (133 error), из
них ~130 — артефакты сломанного конфига (override на `client/**` вместо
`client-nuxt/`, нет vue-parser/Nuxt-глобалов), а не нарушения кода.
Реальных серверных error — 3 (мелочь). Включать lint как блокирующий чек
сейчас НЕЛЬЗЯ — заблокирует все PR. Конфиг чинить рано (фронт переделывается
в Фазе 2/3). Поэтому весь кластер откладывается в Фазу 2/3 вместе с
client-тестами. Бэкенд и так под защитой unit+integration тестов.

#### Задача 1.4.3. External uptime-pinger
- **Что:** настроить UptimeRobot (или подобный) на `/health`
  с интервалом 1-5 минут. Алерты в Telegram при `db:disconnected`.
- **Преимущество:** работает независимо от monitor.sh на самом сервере —
  если упадёт весь VPS, monitor.sh тоже умрёт, а внешний pinger увидит.
- **Ветка:** не нужна (внешняя настройка)

#### Задача 1.4.4. CI-проверка чистоты прод-репо
- **Контекст:** в INC-001 одна из проблем — на проде были несохранённые
  локальные правки (правка docker-compose.yml вручную). Это типичная
  ошибка, нужно её предупредить.
- **Что:** GitHub Action или скрипт, который раз в час делает
  `ssh root@vps "cd /home/questdating && git status --porcelain"` и
  алертит, если рабочее дерево не чистое.
- **Ветка:** `upgrade/prod-repo-check`
- **Альтернатива:** добавить в monitor.sh.

#### Задача 1.4.5. Обновить ESLint конфиги — ⏸️ ОТЛОЖЕНА (часть lint-кластера)
- **Контекст:** node 22, vitest 4.x, обновлённые dependencies — может
  потребовать ревизии ESLint правил.
- **Находки (31 мая, прогон `eslint .`):** конфиг `.eslintrc.js` (ESLint 8,
  старый формат). 333 проблемы, 133 error — из них ~130 артефакты сломанного
  конфига: override настроен на `client/**`, а папка — `client-nuxt/` (Vue-
  правила и vue-parser к фронту не применяются → parse errors на .vue +
  no-undef на Nuxt-автоимпортах). Реальных серверных error — 3 (мелочь).
  Корневого `package-lock.json` нет → в CI только `npm install`, не `npm ci`.
- **Что чинить (когда дойдём):** override `client/**` → `client-nuxt/**`,
  добавить vue-eslint-parser, Nuxt-глобалы, vitest-globals env; привести код
  к 0 errors; затем включать lint в CI.
- **Почему отложена:** см. «Примечание: lint-кластер отложен» выше —
  починка конфига фронта рано, фронт переделывается в Фазе 2/3.
- **Ветка:** `upgrade/eslint-bump` (когда дойдём, вместе с lint-job и 1.3.2)

#### Задачи из INC-002 (nginx-инцидент 28 мая)

Всплыли при падении прода 502 во время деплоя nginx-конфига.
Полный разбор — `docs/incidents.md` (INC-002). Сам конфиг и публичный
`/health` уже починены (PR #29), но осталась гигиена инфраструктуры.

##### ~~Задача 1.4.6. `sites-enabled/quest-dating` → симлинк~~ ✅ DONE (серверная операция, 1 июня)
- **Было:** `enabled/quest-dating` — обычный файл-копия, тогда как
  `n8n.questdating.ru` — симлинк. Рассинхрон available↔enabled.
- **Сделано (по факту, без изменения поведения):** бэкап живого конфига в
  `/tmp`, `available/quest-dating` := точная копия текущего живого конфига
  (НЕ репо-версии — сознательно разделили структурную правку и
  содержательную, чтобы не повторить «два изменения за раз» из INC-002),
  затем `enabled/quest-dating` пересоздан как симлинк на `available/`.
  `nginx -t` ok, reload, три endpoint зелёные (/health db:connected, / 200,
  /api/stats). Теперь правка `available` сразу отражается в активном.
- ⚠️ **Отклонение от исходного сниппета:** план одним куском делал симлинк
  + заливку репо-конфига в available. Это два изменения сразу (структурное
  + содержательное) — ровно ошибка INC-002. Сделано раздельно.

##### ~~Задача 1.4.7. Решить судьбу IP-привязки в `listen`~~ ✅ DONE (PR fix/nginx-listen-strategy)
- **Было (предположение плана):** «IP-привязка хрупка, склониться к
  `listen 443` без IP ради устойчивости к смене IP».
- **Решение (1 июня, по факту nginx -T + ip addr + ss):** **оставить явную
  привязку к `146.103.111.12`** — диагностика перевернула интуицию плана.
  Причины:
  • Активны ровно два vhost, оба на `146.103.111.12`, оба *.questdating.ru
    (liza/questforliza не enabled). На `ens3` два IP: 146.103.111.12
    (DNS-таргет) и 89.124.90.154 (мёртвый для домена — давал 502 в INC-002).
  • AmneziaWG ушёл на amn0 (UDP) → исходная причина IP-привязки (конфликт
    с `0.0.0.0:443`) исчезла, но это НЕ повод снимать привязку — см. ниже.
  • **Footgun:** n8n-блок прибит к `146.103.111.12:443` через
    `certbot --nginx` и ре-пинится при каждом renew. Сделать наш блок
    wildcard при пиннутом n8n → смесь wildcard/специфичного сокета на :443
    → трафик questdating.ru уходит в n8n-блок (специфичный сокет выигрывает)
    → чужой сайт/404 на проде. Wildcard потребовал бы снять n8n с
    certbot --nginx — риск на работающем сервисе без триггера.
  • INC-002 был про *неверный* IP из устаревшего репо, не про сам факт
    привязки. Сейчас репо=реальность, IP корректный, процедура G на месте.
- **Сделано:** привязка не меняется (поведение сервера то же); в
  `nginx/nginx.conf` случайная привязка превращена в задокументированное
  решение с предупреждением «не менять на wildcard при пиннутом n8n» —
  чтобы будущий оператор не «починил» в wildcard и не уронил прод.
  Инвариант: оба questdating-vhost'а на DNS-таргетном IP; смена — только
  при реальной миграции, осознанно.
- **Ветка:** `fix/nginx-listen-strategy` (правка только комментариев + план)

##### ~~Задача 1.4.8. Поправить расхождение SSL в документации~~ ✅ DONE (PR docs/nginx-ssl-deploy)
- **Было:** DEPLOY.md описывал «сертификат из панели рег.ру» + процедуру
  scp pem-файлов в `nginx/ssl/` (раздел 5) — несуществующую. Дерево
  каталогов показывало мёртвую `nginx/ssl/*.pem`.
- **Стало (этим PR, факты проверены на сервере 1 июня):** Let's Encrypt +
  certbot, серт в `/etc/letsencrypt/live/questdating.ru/`, nginx читает
  напрямую (`ssl_certificate .../fullchain.pem`). Автообновление —
  `certbot.timer` (systemd, активен, проверено `systemctl list-timers`).
  Переписаны: шапка DEPLOY.md + раздел 5 (под Let's Encrypt, certbot --nginx,
  renew --dry-run), дерево каталогов DEPLOY.md, дерево ARCHITECTURE.md.
  SETUP.md уже был корректен (правлен в 1.2.4b).

##### ~~Задача 1.4.9. Процедура безопасного деплоя nginx в DEPLOY.md~~ ✅ DONE (PR docs/nginx-ssl-deploy)
- В DEPLOY.md добавлен раздел «G) Изменён nginx-конфиг» (Регулярное
  обновление): бэкап ТОЛЬКО в /tmp (не в sites-enabled/available — иначе
  .bak подхватится вторым server-блоком), обязательный `nginx -t` до
  reload, откат из бэкапа, проверка трёх endpoint после reload (главная +
  /health db:connected + /api). Прямой ответ на INC-002.
- **Объединено с 1.4.8** в один PR (обе про docs + nginx).

### 1.5. Техдолг кода, накопленный по ходу аудита документации

Эти задачи появились **не из исходного аудита**, а всплыли в процессе
работы над 1.2.4 (синхронизация документации с реальностью): когда
сверял доку с кодом, находил несоответствия, которые требуют правок
**в коде или в репозитории**, не в документации.

Все мелкие, но накопились. Закрываются после Фазы 1, до начала Фазы 2.
Каждая — отдельная ветка, отдельный PR; все маленькие.

#### Задача 1.5.1. Унификация маршрутов orders в API — ⏸️ ОТЛОЖЕНА В ФАЗУ 2
- **Решение (31 мая):** откладывается и делается вместе с переделкой API
  заказов в Фазе 2 (AI-опросник заменяет форму заказа → API всё равно
  меняется). Делать аккуратный рефакторинг сейчас, перед переделкой —
  расточительно (та же логика, что и для client-тестов в CI).
- **⚠️ Пометка плана «риск низкий, фронт использует только
  /api/admin/orders/*» — НЕВЕРНА.** Проверка grep по client-nuxt (31 мая)
  показала: фронт использует ОБА пути. Из `routes/orders.js` живые:
  `POST /orders` и `GET /orders/by-token/:token` (публичные, остаются) +
  **`GET /orders/:id` через `useApi.getOrder` — admin-чтение, используется**.
  Дубли в `admin.js` и `orders.js` для PATCH status НЕ идентичны: разная
  валидация (`notEmpty` vs строгий `isIn`), разное поведение (admin_notes +
  нотификация vs нет). Есть подозрительный фронт-метод `updateOrderStatus`
  → `PATCH /admin/orders/:id` (без `/status`), возможно бьёт в никуда.
  При возврате к задаче — разбираться предметно, не по букве плана.
- **Контекст (исходный):** админские операции над заказами имеют два пути:
  `/api/orders/*` (с requireAdmin) и `/api/admin/orders/*` (routes/admin.js).
- **Ветка:** `refactor/unify-orders-routes` (когда дойдём, в Фазе 2)

#### ~~Задача 1.5.2. Привести в порядок `engines` в package.json~~ ✅ DONE (PR chore/sync-engines-readme)
- `server/package.json`: `engines.node` `>=18.0.0` → `>=22.0.0`.
- `client-nuxt/package.json`: добавлено `engines: { node >=22.0.0, npm >=9.0.0 }`.
- **⚠️ Вскрытое расхождение, исправлено тем же PR:** CI (`ci.yml`) гонял
  тесты на `node-version: '20'`, тогда как прод (Dockerfile, доки) — Node 22.
  Выставить `engines >=22` без подъёма CI означало бы EBADENGINE-конфликт.
  Поэтому CI поднят до Node 22 в обоих job — теперь версия консистентна
  везде: Docker, доки, CI, оба манифеста. CI стал тестировать на прод-версии.

#### ~~Задача 1.5.3. Удалить `uploads_local/` из репозитория~~ ✅ DONE (PR chore/cleanup-repo)
- `uploads_local/` (~7.2 МБ, 5 файлов: avatars/media/templates) удалена.
  grep по всему репо (кроме docs) — ноль ссылок в коде/docker/nginx/client/server.
  Tag `pre-cleanup-uploads-local` создан до удаления. В `.gitignore` добавлен
  `uploads_local/`.

#### ~~Задача 1.5.4. Удалить мусорный `dump.sql` из корня репозитория~~ ✅ DONE (PR chore/cleanup-repo)
- **Проверка (31 мая):** корневого `dump.sql` уже нет ни в git, ни на диске
  (удалён ранее). Отдельной правки не потребовал. В `.gitignore` добавлен
  паттерн `/dump.sql` (со слешем — игнорирует ТОЛЬКО корень, не трогает
  рабочий `database/dump.sql`; проверено на изолированном репо), чтобы
  мусорный дамп не закоммитили снова. В репо остались только корректные SQL:
  `database/dump.sql` + 6 миграций.

#### ~~Задача 1.5.5. Привести в порядок README.md~~ ✅ DONE (PR chore/sync-engines-readme)
- Стек-таблица: версии актуализированы (Nuxt 4.x, @nuxtjs/seo 5.x, Vitest 4.x,
  Node 22 LTS, PostgreSQL 15), `Orange Pi 5 Pro` → `VPS на VDSina (vdsina.com)`.
- Требования: Node 20 → 22, PostgreSQL 14+ → 15.
- Добавлен блок про основную ветку `production` (PR + branch protection).
- Ссылки на доки проверены (4 живые), добавлены ARCHITECTURE.md и DEPLOY.md.
- **Контекст (было):** README расходился с реальностью —
  - Инфраструктура: `Orange Pi 5 Pro` → нужно `VPS на VDSina`
  - Стек таблица: не указаны актуальные версии (Node 22, Vitest 4.x,
    @nuxtjs/seo 5.x)
  - Нет упоминания основной ветки `production`
  - Не упоминается quick start через `docker compose up`
- **Что:**
  1. Заменить "Orange Pi 5 Pro" → "VPS на VDSina (vdsina.com)"
  2. Обновить стек таблицу под актуальные версии (как в ARCHITECTURE.md)
  3. Добавить раздел "Основная ветка: production"
  4. Проверить, что ссылки на документы (`docs/SETUP.md` и т.д.) живые
- **Ветка:** `docs/sync-readme`
- **Связано с 1.5.2** (обе мелкие, можно объединить или нет — на твой выбор)

### Критерии готовности Фазы 1 (обновлено)

- [x] Все P0 закрыты, прод задеплоен с новыми env-переменными
- [x] `client-legacy/` удалён
- [x] Lock-файлы трекаются в git
- [x] Docker и хост на Node 22 LTS
- [x] 0 vulnerabilities в обоих package
- [x] `docs/ARCHITECTURE.md` соответствует коду (1.2.4a, PR #20)
- [x] `docs/audit.md` обновлён: пройденные пункты помечены (1.2.4e, PR #23)
- [x] Документация синхронизирована: 6 из 6 файлов (1.2.4 целиком, SETUP — PR #31)
- [x] `database/dump.sql` существует, БД поднимается с нуля (1.2.6, PR #26)
- [x] `docs/SETUP.md` синхронизирован после 1.2.6 (1.2.4b, PR #31)
- [x] nginx-конфиг в репо = реальность, публичный /health работает (PR #29, INC-002)
- [x] Integration-тесты бегают на реальной PostgreSQL (1.2.3, PR #35/#36/#38/#40)
- [x] CI зелёный на ветке `production` (1.3.1 частично — unit+integration; lint/build/hooks осталось)
- [ ] Техдолг кода 1.5 закрыт (5 задач)
- [ ] nginx-гигиена из INC-002 закрыта (1.4.6–1.4.9)

---

## Фаза 2 — AI-опросник через DeepSeek

Цель: заменить статичную форму заказа на интерактивный AI-диалог,
который собирает данные и **автоматически заполняет шаблон квеста**.

### 2.1. Требует brainstorm с Claude

Открытые вопросы для проработки через `/brainstorming`:

1. **Какие поля шаблона квеста заполняет AI?**
   - Текущая структура `quest_template.fields` — какая она?
   - Какие данные клиента нужны для уникализации:
     - Имена партнёров, дата знакомства
     - Локации (любимые места, район проживания)
     - Общие воспоминания, инсайды
     - Тон квеста (романтичный / детектив / приключение)
     - Фото партнёра (для иммерсии в сценах)
   - Какие поля **обязательны**, какие AI может оставить пустыми

2. **Как AI ведёт диалог?**
   - Фиксированный сценарий вопросов или адаптивный?
   - Сколько вопросов? Можно ли пропускать?
   - Что делать, если клиент даёт мусорные ответы или троллит?
   - Прерывание и возврат к диалогу позже

3. **Где запускается DeepSeek — фронт или бэк?**
   - **Бэк:** защита API-ключа, логирование, rate limiting, версионирование промптов
   - MCP-подключение для разработки vs production-вызовы — разные сценарии
   - Стриминг ответов (UX лучше) или ждать полный ответ

4. **Архитектура UX:**
   - Заменяем `order/[templateSlug].vue` или это новая страница `order/[templateSlug]/ai`?
   - Сохранение прогресса при потере соединения (важно — мобильные пользователи)
   - Как клиент видит "что AI понял" перед оплатой
   - Возможность переписать ответ или начать заново

5. **Валидация результата:**
   - AI заполнил поля → клиент видит превью → подтверждает
   - Admin потом может править результат вручную через админку
   - Что если AI заполнил поле некорректно (например, имя партнёра как "Любимая")

### 2.2. Возможная переделка БД (если потребуется)

Если в ходе brainstorm станет ясно, что текущая схема `quest_templates` не
поддерживает нужную структуру полей — здесь же делаем миграцию:

- Добавление таблицы `ai_conversations` (история диалогов)
- Возможно: рефакторинг `quest_template.fields` (jsonb-структура)
- Возможно: новая таблица `quest_orders_ai_data` (что AI собрал отдельно от заказа)

**Решение принимаем после brainstorm.** Если миграция нужна — оформляется
отдельной задачей с обязательным rollback-планом.

### 2.3. Архитектурный скелет (черновой)

```
client-nuxt/app/pages/order/[templateSlug]/ai-chat.vue   ← новый UI
client-nuxt/app/composables/useAiQuestionnaire.js        ← логика диалога
server/src/routes/ai.js                                  ← /api/ai/chat
server/src/services/deepseekService.js                   ← DeepSeek API
server/src/services/questGeneratorService.js             ← парсит ответ → fields
database/migrations/add_ai_conversations.sql             ← хранение истории
```

### 2.4. Этапы

- **2.4.1.** Brainstorm: спецификация полей и сценария диалога
- **2.4.2.** Backend: `/api/ai/chat` endpoint, DeepSeek-интеграция
- **2.4.3.** Backend: парсер ответа AI → `quest_template.fields`
- **2.4.4.** Frontend: чат-интерфейс на `/order/[templateSlug]/ai-chat`
- **2.4.5.** Сохранение истории разговоров в БД
- **2.4.6.** Admin: возможность править результат AI
- **2.4.7.** A/B-тест: старая форма vs AI (опционально)

### Критерии готовности Фазы 2

- [ ] AI-диалог проходит end-to-end на staging
- [ ] Результат корректно ложится в `quest_template.fields`
- [ ] Admin может править после AI
- [ ] Логи всех разговоров доступны для анализа
- [ ] Token usage логируется и видно в админке

---

## Промежуточный этап — Интеграция платежей

Между Фазой 2 и Фазой 3. Логика: к моменту, когда будет первый реальный
сценарий (Фаза 3), нужна возможность брать деньги.

### Что нужно знать сейчас

- Самозанятый → доступен интернет-эквайринг. Это легально, чек автоматически
  отправляется в "Мой налог".
- Сервисы: ЮKassa, Tinkoff Касса, Робокасса, CloudPayments, PayAnyWay —
  все работают с НПД.
- Подключение обычно бесплатное, комиссия 3.5–6% с каждой транзакции.
- **Выбор конкретного сервиса** откладываем до момента, когда дойдём до этой
  задачи — условия и тарифы могут измениться.

### Этапы (черновые, проработаем позже)

- Сравнить актуальные условия 3-4 сервисов на момент начала задачи
- Подписать договор, получить API-ключи
- Backend: `/api/payments/create` + webhook на статус оплаты
- Frontend: страница оплаты + редирект после подтверждения
- Email-уведомление с чеком
- Тест на реальной транзакции

⚠️ Это **не делаем сейчас**. Здесь только зафиксировано, что задача есть.

---

## Фаза 3 — Продукт

Цель: убрать "AI-shaped" дизайн, сделать продукт реально работающим
на мобильных, создать первый настоящий сценарий.

### 3.1. Редизайн плеера квестов

- **Удалить** иммерсивный плеер (компонент + ссылки на него)
- **Убрать** в админке выбор `player_version` (или оставить один)
- **Редизайн** базового плеера: уникальный визуал, mobile-first
- **Требует** дизайн-исследование рынка / референсов

### 3.2. Аудит мини-игр

Пройти каждую мини-игру вручную, **особенно с фото**:

- [ ] Список всех существующих мини-игр (составить из кода)
- [ ] Проверка адаптации фото под мобильные (соотношения, обрезка, lazy)
- [ ] Проверка отображения улик
- [ ] Чек-лист багов и UX-проблем

### 3.3. Первый реальный сценарий

- [ ] Выбрать локацию (свой город / район)
- [ ] Написать сценарий: 5-7 точек, 3-4 улики, финал
- [ ] Сфотографировать улики/локации
- [ ] Залить в админку как шаблон
- [ ] Пройти самому, проверить UX

---

## Хронология (примерная, обновлено 26 мая)

| Этап | Длительность | Зависимости | Статус |
|---|---|---|---|
| 1 — Фундамент | 2-3 недели | — | ~60% (24-26 мая = 2 дня интенсива) |
| 2 — AI-опросник | 3-4 недели | Фаза 1 завершена | not started |
| Платежи | 3-5 дней | Фаза 2 в проде | not started |
| 3 — Продукт | 4+ недель | Платежи подключены | not started |

---

## Правила работы

1. **Каждая задача — отдельная ветка** от `production`, имя `upgrade/<short-name>` или `docs/<short-name>` для документации
2. **Перед `git checkout -b`** — `git checkout production && git pull origin production && git branch --show-current` (проверка)
3. **После `git checkout -b`** — снова `git branch --show-current` (убедиться что переключились)
4. **TDD где возможно:** сначала тест, потом код (используем GSD-подход через Claude Code + Superpowers)
5. **Маленькие коммиты:** один логический шаг = один коммит
6. **Conventional commits format:** `тип(scope): description`
   - типы: feat, fix, refactor, docs, chore, test, style
   - scope опционален: `chore(deps)`, `fix(security)`, `feat(health)`
7. **Перед мержем в `production`:** CI зелёный (когда настроим) + ручная проверка
8. **После мержа:** деплой через `git pull && docker compose build && up -d` на VPS
9. **Перед удалением чего-либо крупного:** git tag для возможности отката (`git tag pre-<what>`)
10. **Перед `npm install`-операцией:** baseline тестов и build, чтобы видеть регрессии

---

## Что НЕ делаем сейчас (явно)

- Не мигрируем на Express 5 (breaking changes, отдельный риск)
- Не пишем e2e на квест-плеер до Фазы 3 (он всё равно будет переделан)
- Не интегрируем платежи в Фазе 1
- Не делаем редизайн до Фазы 3
- Не обновляемся на Node 24 LTS (вышел недавно — пусть устаканится)

---

## Ссылки

- Аудит проекта: `docs/audit.md`
- Журнал инцидентов: `docs/incidents.md` (постмортем INC-001 с разделом
  "Долгосрочный follow-up" — там полный список 18 PR за 24-26 мая)
- Список PR: https://github.com/Never91veUp00/quest-dating/pulls?q=is%3Apr+is%3Aclosed